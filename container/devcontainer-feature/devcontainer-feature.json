{
  "id": "session-tools",
  "version": "0.4.1",
  "name": "Warden Session Infrastructure",
  "description": "Installs abduco, Claude Code CLI, and terminal management scripts for Warden project containers",
  "options": {
    "abducoVersion": {
      "type": "string",
      "default": "0.6",
      "description": "Version of abduco to install"
    },
    "gosuVersion": {
      "type": "string",
      "default": "1.17",
      "description": "Version of gosu to install for entrypoint privilege drop"
    }
  },
  "containerEnv": {
    "WARDEN_MANAGED": "true"
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils"
  ]
}
